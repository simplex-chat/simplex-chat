{
  "name": "simplex-badge-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf build && tsc -p tsconfig.json && tsc -p tsconfig.test.json && node build.js",
    "test": "rm -rf build && tsc -p tsconfig.json && tsc -p tsconfig.test.json && node check-tests.js && node --test 'build/test/**/*.test.js'",
    "mock": "python3 mock/server.py"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^24.0.0"
  }
}
